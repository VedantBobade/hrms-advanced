apiVersion: v1
kind: Service
metadata:
name: {{ include "payroll-service.fullname" . }}
spec:
selector:
app.kubernetes.io/name: {{ include "payroll-service.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
ports:
- port: {{ .Values.service.port }}
targetPort: http
name: http