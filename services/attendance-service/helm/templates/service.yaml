apiVersion: v1
kind: Service
metadata:
name: {{ include "attendance-service.fullname" . }}
spec:
selector:
app.kubernetes.io/name: {{ include "attendance-service.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
ports:
- port: {{ .Values.service.port }}
targetPort: http
name: http